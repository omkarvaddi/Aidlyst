console.log(`Friday desktop packaging path\n\nCurrent mode: local web app + local control server.\nNext packaging step: wrap the Vite UI and Node server with Electron or Tauri.\n\nRecommended path:\n1. Keep server/index.js as the local control layer.\n2. Build the UI with npm run build.\n3. Add Electron main process that starts server/index.js and opens the built UI.\n4. Package with electron-builder for Windows.\n\nThis helper is intentionally informational for now so the local control server remains simple and auditable.`);
